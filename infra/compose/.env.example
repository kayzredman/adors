############################################################
# ADORS Dev Stack — Environment Variables
#
# USAGE:
#   cp infra/compose/.env.example infra/compose/.env
#   # Edit .env with your secrets
#   docker compose -f infra/compose/docker-compose.dev.yml up -d
#
# The JWT keys below are the official Supabase local-dev defaults.
# They pair with JWT_SECRET below. DO NOT use in production.
############################################################

# ── Database ─────────────────────────────────────────────────────────────────
# Password for the Postgres superuser AND all Supabase internal roles
POSTGRES_PASSWORD=supersecret

# ── JWT ──────────────────────────────────────────────────────────────────────
# Must be ≥ 32 characters. Used by GoTrue (auth) and PostgREST.
# Change this before going to production — then regenerate ANON_KEY + SERVICE_ROLE_KEY.
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long

# ── API Keys (JWTs pre-signed for the JWT_SECRET above) ──────────────────────
# Client-side key — safe to expose in browser code
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJhbm9uIiwKICAgICJpc3MiOiAic3VwYWJhc2UtZGVtbyIsCiAgICAiaWF0IjogMTY0MTc2OTIwMCwKICAgICJleHAiOiAxNzk5NTM1NjAwCn0.dc_X5iR_VP_qT0zsiyj_I_OZ2T9FtRU2BBNWN8Bu4GE

# Server-side key — NEVER expose to browsers
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJzZXJ2aWNlX3JvbGUiLAogICAgImlzcyI6ICJzdXBhYmFzZS1kZW1vIiwKICAgICJpYXQiOiAxNjQxNzY5MjAwLAogICAgImV4cCI6IDE3OTk1MzU2MDAKfQ.DaYlNEoUrrEn2Ig7tqibS-PHK5vgusbcbo7X36XVt4Q

# ── URLs ──────────────────────────────────────────────────────────────────────
# External URL Kong is reachable at (used by GoTrue for redirect links in emails)
API_EXTERNAL_URL=http://localhost:8000

# ── Email / SMTP ───────────────────────────────────────────────────────────────
# Leave SMTP_HOST blank to keep MAILER_AUTOCONFIRM=true (no real emails sent).
# Fill these in to enable invite/password-reset emails via GoTrue.
#
# Example providers:
#   Resend:   host=smtp.resend.com  port=587  user=resend  pass=re_xxxx
#   SendGrid: host=smtp.sendgrid.net port=587  user=apikey  pass=SG.xxxx
#   Gmail:    host=smtp.gmail.com   port=587  user=you@gmail.com  pass=app-password
#
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_ADMIN_EMAIL=noreply@yourdomain.com
SMTP_SENDER_NAME=ADORS Mission Control

# Set to false once SMTP is configured — otherwise invites are auto-confirmed
# and no email is sent.
MAILER_AUTOCONFIRM=true

# The public URL of the web app — used in invite/reset email links.
# Must match the domain users click from their inbox.
SITE_URL=http://localhost:3002
SITE_URL=http://localhost:3000
